syntax = "proto3";

package common;

option go_package = "github.com/Biisairo/game_of_life/proto/common;common";

message Empty {}

message Cell {
	int64 x = 1;
	int64 y = 2;
	bool isLive = 3; // is live now
	bool justLive = 4; // just live before
}